
# SentimentScope (Sentiment Analysis)

This project classifies text into **Positive** or **Negative** sentiment using a **TF-IDF + Logistic Regression** model that you trained in Colab.

## Project Contents
- `models/tfidf_logreg.joblib` → Trained model
- `sentiment_analysis.ipynb` → Training notebook
- `requirements.txt` → Libraries list (will add soon)
- UI (Gradio/Streamlit) → Coming in next steps

## Dataset Format
Your dataset must be in CSV format:


## Next Steps
1. Upload this project to GitHub
2. Create a simple **UI**
3. Deploy it permanently (Hugging Face Spaces)
